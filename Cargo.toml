# === Cargo.toml ===
[package]
name = "RustyBoot"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

lto = true
codegen-units = 1

[[bin]]
name = "RustyBoot"
test = false
bench = false
